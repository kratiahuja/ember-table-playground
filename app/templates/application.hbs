{{outlet}}

<WalletSavedViewTable>
  <:table-actions as |cell|>
    <!-- Remove logging and add your additional custom actions other than edit/delete/set-as-default -->
    {{log cell.value}}
    {{log cell.row}}
    {{log cell.column}}
  </:table-actions>
</WalletSavedViewTable>